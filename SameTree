/*
Given the root of two binary trees p and q,write a function to check if they are structurally same or not.
Two Binary Trees are considered same if theuy are structurally identical and the nodes have the same value.


Input:
  p=[1,2,3], q=[1,2,3]
Output:
  true
*/
class Solution{
  public:
   bool isSameTree(TreeNode*p, TreeNode*q){
     if(p == NULL && q == NULL){
        return true;
     } 
     if(p == NULL || q == NULL){
       return false;
     }
     return (p->val == q->val && isSameTree(p->left, q->left) && isSameTree(p->right, q->right));
   }
 };
